name: image_picker_web_example
description: Demonstrates how to use the image_picker_web plugin.
publish_to: "none"

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  video_player: ^2.4.7

dev_dependencies:
  flutter_test:
    sdk: flutter

  image_picker_web:
    path: ../

flutter:
  uses-material-design: true
